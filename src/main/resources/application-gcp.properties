# GCP production profile properties
# Keep logging levels as defined in application.properties; logback-spring.xml will
# ensure only console output is used when 'gcp' profile is active.

# (Optional) override LOGS_PATH if needed; left empty since GCP uses console only
# LOGS_PATH=/var/log/trainingapp

# Any other production-specific overrides can be added here
